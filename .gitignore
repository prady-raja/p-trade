# Environment files — NEVER commit these
.env
.env.local
backend/.env
frontend/.env
frontend/.env.local

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
backend/.venv/
backend/__pycache__/

# Node
node_modules/
frontend/node_modules/
frontend/.next/
frontend/out/

# Test artifacts
.coverage
htmlcov/
coverage/

# macOS
.DS_Store

# SQLite database files — never commit
*.db
ptrade.db

# Git hooks are installed locally, not committed
# (the scripts/ versions ARE committed as the source of truth)