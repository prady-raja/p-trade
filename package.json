{
  "name": "p-trade",
  "private": true,
  "scripts": {
    "test:frontend": "jest tests/frontend",
    "test:backend": "bash -c 'source backend/.venv/bin/activate && pytest tests/backend -v'"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/tests/frontend/**/*.test.ts"]
  }
}
